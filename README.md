# Ada Voices API

Backend API and mock database for the Ada Voices platform prototype. Built with Node.js and Express.

## Setup

1. Install dependencies:
   `npm install`

2. Start the development server:
   `npm start`
   (Server runs on http://localhost:3000)

## Testing

Run the Jest test suite to validate API endpoints, XSS prevention, and form validation:
`npm test`